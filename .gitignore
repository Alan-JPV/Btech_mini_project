# 🔹 Node Modules (DO NOT push this, it can be installed via `npm install`)
node_modules/

# 🔹 Environment Variables (Contains secrets like DB passwords & API keys)
.env

# 🔹 Firebase Private Key (VERY IMPORTANT – Contains sensitive credentials)
firebase-key.json

# 🔹 Log Files
*.log
logs/
npm-debug.log
yarn-debug.log
yarn-error.log

# 🔹 OS / Editor Files (Unnecessary files from different OS and editors)
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# 🔹 Build Files (Generated files that don’t need to be pushed)
dist/
coverage/

# 🔹 MongoDB Data (If you accidentally have local DB files)
*.pem
dump/
